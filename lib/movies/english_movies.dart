import 'package:flutter/material.dart';

class EnglishMovies extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('English Movies')),
      body: Center(child: Text('List of English Movies')),
    );
  }
}
